name = "Nanaloveyuki/parsec"

version = "0.1.1"

description = "Composable, token-generic parsers for MoonBit."

repository = "https://github.com/Nanaloveyuki/parsec"

license = "Apache-2.0"

readme = "README.md"

keywords = [ "moonbit", "parser", "parser-combinator", "parsing" ]

preferred_target = "wasm-gc"

source = "src"
